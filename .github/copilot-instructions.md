# Copilot Instructions

## Project Guidelines
- User prefers to keep chunked-compression .cpp/.h files in CMake source lists unconditionally and make their contents empty/disabled with preprocessor #ifdefs, rather than conditionally adding/removing files in CMake.